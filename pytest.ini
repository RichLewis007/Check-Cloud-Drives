[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --strict-markers
    --tb=short
markers =
    qt: marks tests that require Qt application (pytest-qt)
    slow: marks tests as slow (deselect with '-m "not slow"')

